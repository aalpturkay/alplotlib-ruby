<div id="<%= dom_id post %>" class="w-full sm:w-auto my-5 space-y-5">
  <h1 class="mb-3 text-4xl font-bold"><%= post.title %></h1>

  <div class="mb-8 text-sm text-gray-500">
    <span class="font-medium text-gray-700"><%= post.user.first_name %></span> •
    <time datetime="2025-04-12"><%= post.published_at.to_formatted_s(:short) %></time>
  </div>

  <div class="mb-12 space-y-5 text-lg leading-relaxed">
    <%= post.body %>
  </div>

  <div>
    <strong class="block font-medium mb-1">Durum:</strong>
    <%= post.status.humanize %>
  </div>
</div>
