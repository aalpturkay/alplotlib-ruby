<nav data-controller="navbar" class="flex items-center justify-between md:px-24 px-6 py-4 border-b border-gray-200 bg-white w-full z-50 top-0">
  <div class="text-xl font-bold tracking-wide">
    <%= link_to "alplotlib", root_path, class: "font-mono" %>
  </div>

  <div class="flex items-center gap-4">
    <% if authenticated? %>
      <%= button_to "Çıkış Yap", session_path, method: :delete, class: "hidden md:block bg-neutral-800 text-white px-4 py-2 rounded-full text-sm" %>
    <% else %>
      <%= link_to "Giriş Yap", new_session_path, class: "hidden md:block bg-neutral-800 text-white px-4 py-2 rounded-full text-sm" %>
    <% end %>

    <!-- Hamburger Button -->
    <button data-action="click->navbar#openMenu" class="md:hidden focus:ring-black focus:ring-2 focus:rounded-sm">
      <svg class="w-6 h-6 text-gray-800" fill="none" stroke="currentColor" stroke-width="2"
        viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
        <path d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </div>

  <!-- Mobile Menu -->

  <div data-navbar-target="menu"
      class="fixed inset-0 z-40 hidden flex items-end justify-center bg-black/50 backdrop-blur-sm">
    <div data-navbar-target="content"
        class="flex flex-col items-center justify-center bg-white border m-4 rounded-full p-6 w-full max-w-md text-center space-y-4 shadow-xl transform transition duration-300 scale-95 opacity-0 translate-y-[50px]">
      <% if not authenticated? %>
          <%= link_to "Giriş Yap", new_session_path, class: "transform cursor-pointer bg-neutral-600 p-2 font-mono font-black tracking-widest text-white transition duration-300 hover:scale-110 hover:bg-neutral-400 focus:ring-1 focus:ring-black focus:ring-offset-2 focus:ring-offset-transparent focus:outline-none" %>
      <% end %>
      <%= button_to "Çıkış Yap", session_path, method: :delete, class: "transform cursor-pointer bg-neutral-600 p-2 font-mono font-black tracking-widest text-white transition duration-300 hover:scale-110 hover:bg-neutral-400 focus:ring-1 focus:ring-black focus:ring-offset-2 focus:ring-offset-transparent focus:outline-none" if authenticated? %>


      <button data-action="click->navbar#closeMenu" class="text-sm text-gray-500 hover:text-black">Kapat</button>
    </div>
  </div>

</nav>

